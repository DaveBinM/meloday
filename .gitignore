logs/lyric_batch_state.json
logs/lyric_backfill.log
assets/mood_mix_history.json
assets/geo_scene_rotation.json
# Regenerable per-artist maps (built by pre_analyze syncs) — not tracked
assets/mbid_artist_names.json
assets/lastfm_artist_top_tracks.json

# Runtime single-instance lockfiles (created by single_instance_guard) — never commit
assets/locks/

# Dev-only: holds the real Plex token + OpenAI key — never commit
live/
